import React from 'react';

import './ArticlesList.scss';

export const ArticlesList = () => {

    return (
        <div className="page">
            ArticlesList
        </div>
    );
};

export default ArticlesList;
